# 精准动态教辅系统 - 环境变量配置示例
# 复制此文件为 .env 并填入真实的配置值

# ===========================================
# AI模型API配置
# ===========================================

# 通义千问 (阿里云灵积) API配置
# 获取地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your-dashscope-api-key-here

# DeepSeek API配置  
# 获取地址: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# ===========================================
# 数据库配置
# ===========================================

# 数据库连接URL (默认使用SQLite)
DATABASE_URL=sqlite:///./dongtaijf.db

# PostgreSQL示例 (如需使用)
# DATABASE_URL=postgresql://username:password@localhost:5432/dongtaijf

# MySQL示例 (如需使用)  
# DATABASE_URL=mysql+pymysql://username:password@localhost:3306/dongtaijf

# ===========================================
# 安全配置
# ===========================================

# JWT密钥 (请修改为随机字符串)
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# API密钥加密密钥 (请修改为随机字符串)
ENCRYPTION_KEY=your-encryption-key-for-api-keys-change-this

# ===========================================
# 应用配置
# ===========================================

# 应用运行环境
ENVIRONMENT=development

# 调试模式
DEBUG=true

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ===========================================
# 文件存储配置
# ===========================================

# 上传文件存储路径
UPLOAD_PATH=./uploads

# PDF文件存储路径
PDF_PATH=./pdfs

# 静态文件路径
STATIC_PATH=./static

# ===========================================
# AI模型参数配置
# ===========================================

# 默认AI模型
DEFAULT_AI_MODEL=tongyi

# 通义千问模型参数
TONGYI_MODEL=qwen-turbo
TONGYI_MAX_TOKENS=2000
TONGYI_TEMPERATURE=0.7

# DeepSeek模型参数
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_MAX_TOKENS=2000
DEEPSEEK_TEMPERATURE=0.7

# ===========================================
# 系统限制配置
# ===========================================

# 单次上传文件大小限制 (MB)
MAX_FILE_SIZE=10

# 单次生成题目数量限制
MAX_QUESTIONS_PER_REQUEST=20

# API调用频率限制 (每分钟)
API_RATE_LIMIT=60

# ===========================================
# 邮件配置 (可选)
# ===========================================

# SMTP服务器配置
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your-email@example.com
SMTP_PASSWORD=your-email-password
SMTP_USE_TLS=true

# ===========================================
# Redis配置 (可选，用于缓存)
# ===========================================

# Redis连接URL
REDIS_URL=redis://localhost:6379/0

# ===========================================
# 监控和分析配置 (可选)
# ===========================================

# Sentry错误监控
SENTRY_DSN=your-sentry-dsn-here

# Google Analytics
GA_TRACKING_ID=your-ga-tracking-id

# ===========================================
# 第三方服务配置 (可选)
# ===========================================

# 阿里云OSS (对象存储)
ALIYUN_OSS_ACCESS_KEY_ID=your-oss-access-key
ALIYUN_OSS_ACCESS_KEY_SECRET=your-oss-secret-key
ALIYUN_OSS_BUCKET=your-bucket-name
ALIYUN_OSS_ENDPOINT=your-oss-endpoint

# 腾讯云COS (对象存储)
TENCENT_COS_SECRET_ID=your-cos-secret-id
TENCENT_COS_SECRET_KEY=your-cos-secret-key
TENCENT_COS_BUCKET=your-bucket-name
TENCENT_COS_REGION=your-cos-region